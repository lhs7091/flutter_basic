import 'package:flutter/material.dart';

class GetXFirstPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('GetXFirstPage'),
      ),
    );
  }
}
